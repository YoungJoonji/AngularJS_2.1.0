describe('App component test', () => {
    it('Should get 5 alphabets', () => {
        let list = ['a', 'b', 'c', 'd', 'e'];

        expect(list.length).toBe(5);
    });

    it('Should get 5 alphabets', () => {
        let list = ['a', 'b', 'c', 'd', 'e'];

        expect(list.length).toBe(5);
    });
});
